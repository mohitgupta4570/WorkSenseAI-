# Runtime dependencies for app/streamlit_app.py.
# Training-only packages (gensim, hmmlearn, torch, etc.) are intentionally
# excluded from the Streamlit deployment image.
pandas
numpy
scikit-learn
spacy
plotly
streamlit
joblib
